import { UserPlus } from "lucide-react";
import type { ButtonConfig } from "./types/button";

export const buttonConfig: ButtonConfig = {
  signup: { label: "Sign Up", icon: UserPlus, actionUrl: "/signup" },
};
